---
import { FeaturesItemParagraph, Heading2Small } from '@components/atoms/index.astro';

interface Props {
  heading: string;
  icon: any;
  paragraph: string;
}

const {  heading, icon: Icon, paragraph } = Astro.props as Props;
---

<Icon />
<Heading2Small text={heading} />
<FeaturesItemParagraph text={paragraph} />
